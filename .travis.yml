language: python
python:
  - 2.7
  - 3.3
  - 3.4
  - 3.5
install:
  - pip install coveralls
  - pip install .
script:
  - coverage run --branch --source=pyluac pyluac/test_suite.py
after_success:
  - coveralls
  - coverage report -m
